week02

-linux command : ls, ls -al, pwd, cd .git, cd .. , echo "blahblah" >> README.md, cat README.md

-git command 
=> 
git init

git status

git add week01.txt

git config --global user.email "you@example.com"

git config --global user.name "your name"

git commit -m "v0"

git log : 현재 확인하기

git checkout hashcode

git checkout -

git branch -M main